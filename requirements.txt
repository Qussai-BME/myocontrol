# MyoControl Suite v0.5 — Requirements
#
# For minimal install (no UI/API/ONNX/PyTorch):
#   pip install -e .                          (core only)
#   pip install -e ".[ui,api,onnx,torch]"     (everything)
# See pyproject.toml for optional extras groups.

# Core
numpy>=1.21
scipy>=1.7
pandas>=1.3
scikit-learn>=1.0

# UI
streamlit>=1.20
plotly>=5.0

# API
fastapi>=0.85
uvicorn>=0.18
python-multipart>=0.0.5
pydantic>=1.10

# ML
xgboost>=1.6
shap>=0.41
numba>=0.57  # JIT acceleration

# PDF clinical reports
reportlab>=4.0
pypdf>=4.0
matplotlib>=3.5

# ONNX Edge deployment
onnx>=1.14
onnxruntime>=1.16
skl2onnx>=1.16
onnxmltools>=1.11

# PyTorch (for Lite-DAN, optional — CPU-only is fine)
torch>=2.0

# Utilities
psutil>=5.8

# Development
pytest>=7.0
pytest-cov>=4.0
